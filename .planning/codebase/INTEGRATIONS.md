# Integrations

- **ImgBB API:** Utilizada via script Python (`migrar_posts.py`) para fazer o upload de imagens antigas do Blogger e hospedar permanentemente, gerando links retornados via API para injetar no Markdown estático.
- **Blogger (XML Export):** Fonte de dados original migrada offline.
